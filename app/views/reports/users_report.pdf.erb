<h1>
  Reporte de Cantidad de publicaciones por usuario
</h1>
<table class="table">
  <tr>
    <th>Id</th>
    <th>Nombre</th>
    <th>Apellido</th>
    <th>Cantidad de publicaciones</th>
    <th>Porcentaje de participación</th>
  </tr>

  <tbody>
    <% @reports_users.each do |post| %>
      <% num_p_by_rg = Publication.num_publications_by_user(post.id)%>
      <% perc = (num_p_by_rg * 100 / Publication.all.count) %>
      <tr>
        <td><%= post.id %></td>
        <td><%= post.name %></td>
        <td><%= post.lastname %></td>
        <td><%= num_p_by_rg %></td>
        <td><%= "#{perc}%" %></td>
      </tr>
    <%end%>
  </tbody>
</table>
